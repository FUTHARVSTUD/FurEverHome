# Backend service secrets
JWT_SECRET=replace-with-strong-secret

# MongoDB connection string used by the backend container
MONGO_URI=mongodb://appuser:strongpassword@host.docker.internal:27017/fureverhome?authSource=admin
# If you prefer running the bundled MongoDB container, comment the line above
# and provide the root credentials below instead.
# MONGO_ROOT_USER=admin
# MONGO_ROOT_PASSWORD=replace-with-strong-password

# Frontend build-time configuration
REACT_APP_API_BASE=http://your-domain-or-public-dns/api

# Optional: restrict backend CORS (defaults to *)
CORS_ORIGIN=http://your-domain-or-public-dns
